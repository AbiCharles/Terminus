Finally, add a `track` subcommand so `python /app/signal_analysis.py track` records each valid experiment's analysis as its own run in MLflow. Use a local sqlite tracking store at sqlite:////app/mlflow.db with artifacts under /app/mlruns, and put the runs under an experiment named signal-analysis. Same defaults and optional --experiment-id scoping as before; the data still comes only from /app/measurements.duckdb.

Name each run after its experiment id and log the analysis you computed in the previous step. Log params for experiment_id, sample_rate_hz, n_samples, detrend, the bootstrap seed (mc_seed), iteration count (mc_iterations), and confidence level (confidence_level). Log metrics for std, rms, skew, kurtosis, min, max, the three dominant frequencies as dominant_freq_1_hz, dominant_freq_2_hz, dominant_freq_3_hz, and the RMS confidence interval as ci_rms_low, ci_rms_high, ci_rms_width. Set a tag experiment_id to the experiment id and a tag tool to duckdb-signal-analysis. Finally attach that experiment's measurements.csv and analysis.json as run artifacts under an analysis path.
