Milestone 2: build the `components` table defined in `/app/spec.md` — the strongly connected components of the graph. Two nodes share a component when each can reach the other, and every node shares a component with itself. Label each node with `component_id` equal to the smallest `node_id` in its component, one row per node. Write it back into `/app/graph.duckdb`.
