Milestone 3: build the `sessions` table defined in `/app/spec.md`. Group each node's `activations` into sessions ordered by time, starting a new session whenever the gap from the previous activation is strictly greater than 30 minutes (a gap of exactly 30 minutes stays in the same session). For each session record its index (per node, starting at 1), its start and end timestamps, and its event count. Write it back into `/app/graph.duckdb`.
