Milestone 3: build the `shortest_cycles` table defined in `/app/spec.md`. For every node that lies on a directed cycle, compute the length (number of edges) of the shortest directed cycle through it — the fewest edges on a path that leaves the node and returns to it. Nodes that are not on any cycle must not appear. Write it back into `/app/graph.duckdb`.
