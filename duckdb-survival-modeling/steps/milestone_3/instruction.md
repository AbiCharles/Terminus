Milestone 3: with the saved model, score the test-split subjects and write the results back into `/app/survival.duckdb` as the `survival_curves`, `risk_scores`, and `model_metrics` tables defined in `/app/spec.md`, where `model_metrics` holds `c_index` and `integrated_brier_score`. Also write the predictions file to `/app/artifacts/predictions.csv`. The C-index and Integrated Brier Score must be computed exactly as the spec defines them (censoring-aware, IPCW), and the saved model must reproduce the written survival curves when reloaded.
