Milestone 3: with the saved model, score the test-split subjects over all periods and write the results back into `/app/survival.duckdb` as the `cif_curves`, `risk_scores`, and `model_metrics` tables defined in `/app/spec.md`, where `model_metrics` holds `c_index` and `integrated_brier_score`. Also write `/app/artifacts/predictions.csv`. The cumulative incidence functions, the cause-1 concordance index, and the competing-risks IPCW Integrated Brier Score must be computed exactly as the spec defines them, and the saved model must reproduce the written curves when reloaded.
