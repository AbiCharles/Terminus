Milestone 2: retrieve experiment 1's observations from the API at `http://127.0.0.1:8000` and write `/app/output/observations.csv` as defined in `/app/spec.md`. The readings are paginated and raw — fetch every page, drop the invalid readings, convert each remaining reading to physical units (seconds and centimetres) using the sensor calibration, and write the rows with header `time_s,height_cm` sorted by time.
