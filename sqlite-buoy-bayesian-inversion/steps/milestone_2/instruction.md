Next, add a `query` subcommand so `python /app/buoy_calibrate.py query` reads /app/observations.db together with the /app/protocol.json from the previous step and writes, for each calibrated buoy, /app/artifacts/<buoy_id>/clean.csv. A buoy is calibrated only when its status equals the protocol's active status and its sensor type is one of the included types; everything else is skipped entirely.

For each calibrated buoy, drop any observation whose timestamp falls inside an exclusion interval that applies to it (intervals with buoy_id "ALL" apply to every buoy, and the windows are half-open — a reading at the start instant is excluded, one at the end instant is kept). Convert each remaining raw reading to physical units with that sensor type's unit conversion, and compute elapsed time in days since the mission epoch. Write clean.csv with the columns timestamp,t_days,converted_value,reference_value,residual ordered by timestamp, where residual is converted_value minus reference_value (the reference_reading column from the database is already in physical units).
